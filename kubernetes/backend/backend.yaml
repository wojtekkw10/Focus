apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: focus-backend
  name: focus-backend
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 3
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: focus-backend
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: focus-backend
    spec:
      containers:
      - image: gcr.io/vigilant-walker-327408/github.com/wojtekkw10/backend/focus:132f1e0
        imagePullPolicy: IfNotPresent
        name: focus-1
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30