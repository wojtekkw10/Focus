apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: focus-frontend
  name: focus-frontend
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 3
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: focus-frontend
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: focus-frontend
    spec:
      containers:
      - image: gcr.io/vigilant-walker-327408/github.com/wojtekkw10/frontend/focus:132f1e0
        imagePullPolicy: IfNotPresent
        name: focus-1
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
